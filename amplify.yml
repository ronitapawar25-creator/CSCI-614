version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No frontend build step required"
    build:
      commands:
        - echo "Static site ready"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
